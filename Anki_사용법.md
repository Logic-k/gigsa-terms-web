# 정보처리기사 용어사전 — Anki 사용법

**파일** `정보처리기사_용어사전_Anki.apkg` (2.9MB)
**노트 2,263개 / 카드 2,263장** · **실무 예시 1,008개**와 도식 464개가 카드 안에 그대로 들어 있습니다.

---

## 1. 가져오기

| 기기 | 방법 |
|---|---|
| PC (Anki) | 파일을 더블클릭, 또는 Anki 실행 → **파일 → 가져오기** |
| Android (AnkiDroid) | 파일 관리자에서 `.apkg` 를 탭 → AnkiDroid 선택 |
| iPhone (AnkiMobile) | 파일 앱에서 공유 → AnkiMobile |
| 웹 (AnkiWeb) | PC 에서 가져온 뒤 동기화하면 폰에도 내려옵니다 |

가져오면 **정보처리기사 용어사전** 덱 아래에 과목별 하위덱 5개가 생깁니다.

```
정보처리기사 용어사전
├── 1과목 소프트웨어 설계        436장
├── 2과목 소프트웨어 개발        415장
├── 3과목 데이터베이스 구축      376장
├── 4과목 프로그래밍 언어 활용    543장
└── 5과목 정보시스템 구축관리     493장
```

---

## 2. 카드 4종

| 노트 타입 | 장수 | 앞면 | 뒷면 |
|---|---:|---|---|
| **정처기 용어** | 1,008 | 용어 + 영문 | 한 줄 정의 · 상세 설명 · **예시(사례+고리)** · 도식 · 혼동 용어 · 출제 형태 · 위치 |
| **정처기 혼동 짝 구별** | 829 | `A vs B — 무엇이 다른가?` | 두 용어 좌우 대조 + **각 용어의 사례** · 시험이 파는 함정 · 도식 |
| **정처기 도식 재생** | 254 | 용어 + `순서대로 나열하십시오` 같은 지시 + 힌트(항목 수) | 도식 · **사례** |
| **정처기 약어→풀네임** | 172 | `ICMP` | `Internet Control Message Protocol` + 한 줄 정의 · **사례** |

### 예시는 두 줄입니다

- **사례** — 어디서 실제로 쓰이는지. 예: MOM 카드에 「주문이 들어오면 Kafka에 메시지를 쌓아 두고 정산 서버가 나중에 꺼내 처리한다.」
- **고리** — 단어나 일상에 붙여 기억하는 장치. 같은 카드에 「우체통에 넣고 가면 끝 — 받는 쪽이 지금 없어도 된다.」

떠오르지 않을 때 사례를 먼저 읽고 정의를 되짚는 순서로 쓰시면 됩니다.

야간 모드(어두운 테마)에서도 도식이 정상으로 보이도록 색을 따로 정의해 두었습니다.

---

## 3. 2,263장은 많습니다 — 이렇게 줄여 시작하세요

Anki 기본 설정(하루 신규 20장)이면 113일이 걸립니다. 아래 중 하나로 시작하는 것을 권합니다.

### 방법 A — 내가 틀린 개념부터 (503장, 추천)

오답노트 2회분에 등장한 개념에 `오답노트출처` 태그를 붙여 두었습니다. **찾아보기(Browse)** 검색창에 붙여넣고 나머지를 일시 정지(Suspend)하세요.

```
deck:"정보처리기사 용어사전::*" -tag:오답노트출처
```
→ 전체 선택 후 우클릭 → **Toggle Suspend** (단축키 `Ctrl+J`)

### 방법 B — 보조 혼동짝만 빼기 (1,749장)

혼동 짝 829개 중 출제 문구에 실제로 "바꿔 낸다·혼동·구별" 이 언급된 315개는 `혼동짝_핵심`, 나머지 514개는 `혼동짝_보조` 로 나눠 두었습니다.

```
tag:혼동짝_보조
```
→ 일시 정지

### 방법 C — 시험 과목 순서대로

한 과목씩만 공부(Study)하고 나머지 하위덱은 건드리지 않으면 됩니다.

### 일시 정지를 되돌리려면

같은 검색으로 다시 찾아 `Ctrl+J` 를 한 번 더 누르면 풀립니다.

---

## 4. 태그 전체 목록

| 태그 | 개수 | 뜻 |
|---|---:|---|
| `유형_용어` | 1,008 | 용어→설명 카드 |
| `유형_혼동짝` | 829 | 짝 구별 카드 |
| `유형_도식` | 254 | 도식 재생 카드 |
| `유형_약어` | 172 | 약어→풀네임 카드 |
| `과목1` ~ `과목5` | 436·415·376·543·493 | 과목 |
| `챕터_1_1` ~ `챕터_5_12` | 69종 | 출제기준 챕터 |
| `오답노트출처` | 503 | 제공하신 오답노트에 나온 개념 |
| `도식있음` | 464 | 뒷면에 도식이 붙은 용어 카드 |
| `예시있음` | 1,008 | 사례·고리가 붙은 용어 카드 (전부) |
| `혼동짝_핵심` / `혼동짝_보조` | 315 / 514 | 짝의 우선순위 |
| `도식_stack`, `도식_chain`, `도식_grid` … | 10종 | 도식 유형별 |

**쓸모 있는 검색 예시**

```
tag:과목3 tag:유형_혼동짝            3과목 혼동 짝만
tag:도식_rank                       순서 암기(결합도·응집도·정규화 등)만
tag:챕터_4_9                        가상기억장치 챕터만
is:due tag:오답노트출처              오늘 볼 것 중 내 약점만
prop:lapses>3                       세 번 이상 잊은 카드만
```

---

## 5. 함께 넣은 TSV 파일

노트 타입을 직접 만들고 싶거나 다른 앱에 넣고 싶을 때 쓰는 텍스트 파일입니다. HTML·도식은 제거되고 순수 텍스트만 들어 있습니다.

| 파일 | 행 | 열 |
|---|---:|---|
| `정보처리기사_Anki_용어.tsv` | 1,008 | 용어 · 영문 · 한줄정의 · 상세설명 · **사례** · **고리** · 혼동용어 · 출제형태 · 위치 · 태그 |
| `정보처리기사_Anki_혼동짝.tsv` | 829 | 짝 · 용어A · 정의A · 상세A · **사례A** · 용어B · 정의B · 상세B · **사례B** · 함정 · 위치 · 태그 |
| `정보처리기사_Anki_약어.tsv` | 172 | 약어 · 풀네임 · 한줄정의 · **사례** · 위치 · 태그 |

첫 3줄에 `#separator:tab` 등 Anki 가 읽는 지시자가 들어 있으니 그대로 가져오면 됩니다.

---

## 6. 내용을 고치거나 다시 만들려면

`anki-build/` 폴더에 생성 스크립트가 있습니다.

```
anki-build/extract.js   용어사전(data.js) + 도식(dia-data-*.js) + 예시(ex-data-*.js) → source.json
anki-build/build.py     source.json → .apkg   (genanki 필요: pip install genanki)
anki-build/tsv.py       source.json → TSV
```

용어사전 쪽(`gigsa-site/data.js`)을 고친 뒤 `node extract.js && python3 build.py` 를 다시 돌리면 됩니다.

**노트 GUID 가 용어 이름으로 고정되어 있으므로**, 다시 가져와도 카드가 중복 생성되지 않고 **내용만 갱신되며 학습 이력(복습 기록)은 그대로 유지**됩니다. 단 용어 이름을 바꾸면 새 카드로 취급됩니다.

---

## 7. 이미 v1 을 가져오셨다면

노트 타입 ID 를 그대로 두고 필드만 뒤에 추가했습니다. 그래서 같은 파일을 다시 가져오면 **노트 타입이 갱신되고 기존 카드에 예시 항목이 붙으며, 복습 이력은 그대로 유지**됩니다. 노트 GUID 도 용어 이름 기준이라 중복 생성되지 않습니다.

가져올 때 Anki 가 「노트 타입이 변경되었습니다」 같은 안내를 띄울 수 있습니다. 그대로 진행하시면 됩니다. 혹시 꺼림칙하시면 가져오기 전에 **도구 → 백업 만들기**를 한 번 해 두세요.

---

## 8. 자체 검증 리포트

**통과**

- 덱 5개 · 노트타입 4개 · 노트 2,263개 · 카드 2,263개, 카드-노트 정합성 이상 0건
- 필드 수 불일치 0 · 정렬 필드 비어 있음 0 · 같은 노트타입 내 정렬 필드 중복 0 · 필드 구분자 누출 0
- **예시 채움률 100%** — 용어 카드 사례 1,008/1,008 · 고리 1,008/1,008, 혼동 짝 사례A 829 · 사례B 829, 도식 카드 254/254, 약어 카드 172/172
- 모든 카드의 앞/뒷면 **4,526건을 실제로 치환 렌더해 HTML 구조 검사** → 미치환 플레이스홀더 0건, 닫히지 않은 태그 0건
- 카드에 포함된 inline SVG 39개 전부 파싱 성공
- CSS 에 `color-mix()` · `env()` 등 구형 AnkiDroid 웹뷰가 못 읽는 함수 사용 0건
- 모든 카드가 신규 상태(type=0, queue=0)로 생성 — 가져오는 즉시 정상 학습 가능
- 용어사전 쪽에서도 1,008개 전 용어에 예시가 붙었는지 별도로 대조 → 누락 0 · 초과 키 0 · 형식 이상 0 · 사례 문구 중복 0

**검증 과정에서 실제로 고친 것**

- 혼동 짝 카드의 우선순위 분류가 처음에는 69개만 `핵심`으로 잡혔습니다. 판정 근거를 상세 설명이 아니라 **출제 형태** 문장으로 바꿔 315개로 교정했습니다.
- 처음 검사에서 짝 카드 829건이 전부 실패로 나왔는데, 원인은 제가 쓴 검사기가 XML 기준이라 `<br>` 를 거부한 것이었습니다. HTML5 기준 검사기로 바꿔 재검증한 결과 0건입니다. **카드 자체에는 문제가 없었습니다.**

**알려진 한계**

- **예시는 제 지식으로 작성했습니다.** 1,008개 용어를 각각 웹 검색하는 것은 현실적이지 않아, 실무 사례는 제가 아는 실제 사용 맥락으로 쓰고 **사실이 낡을 수 있는 항목만 표적 검증**했습니다(CI 도구 점유율 현황, WPA2 KRACK 취약점과 WPA3 SAE 등). 제품 점유율·버전 같은 수치는 시간이 지나면 달라집니다.
- **암기 고리는 시험 용어가 아닙니다.** 기억을 붙잡기 위한 장치이므로 답안에는 정식 용어를 쓰셔야 합니다.
- 덱 옵션(하루 신규 카드 수, 학습 간격)은 `.apkg` 에 담기지 않습니다. Anki 의 기본 프리셋이 적용되므로 위 3장을 참고해 직접 조절하세요.
- 도식은 필드에 HTML/SVG 로 들어 있습니다. **Anki 편집기에서 그 필드를 클릭해 수정하면 구조가 깨질 수 있습니다.** 도식을 바꾸려면 `dia-data-*.js` 를 고쳐 다시 생성하는 편이 안전합니다.
- 카드 방향은 **용어 → 설명** 한 방향입니다. 반대 방향(설명 → 용어)이 필요하면 노트 타입에 카드 템플릿을 하나 더 추가하면 되고, 그러면 카드 수가 2배가 됩니다.
- `오답노트출처` 태그는 오답노트 60문항의 핵심 용어 목록을 기준으로 붙였습니다. 그 문항이 직접 묻지 않았지만 같은 챕터에 있는 인접 용어까지 포함되어 실제 503장으로 넓게 잡혀 있습니다.
